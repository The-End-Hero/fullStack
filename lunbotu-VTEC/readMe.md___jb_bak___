#轮播图小插件说明
###基于jQurey1.0版本，兼容IE6、7、8
####V1.0输入参数（图片数量,需要插入的div的类名），动态生成轮播图
####图片名称为"l"+num+'.jpg',例如l0.jpg.
>$(function () {
>        //num为图片个数
>        var num=6
>        //origin是需要插入轮播图的div的类名
>        var origin=document.querySelector('.lunbotu-origin').className
>        lunbotu(num,origin)
>        autoMove(num)
>        xiabiao()
>   })
